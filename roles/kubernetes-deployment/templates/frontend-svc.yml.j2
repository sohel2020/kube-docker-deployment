apiVersion: v1
kind: Service
metadata:
  name: laravel
  labels:
    app: laravel
  namespace: {{ git_branch }}  
spec:
  ports:
    - port: 80
  selector:
    app: laravel
    tier: frontend
  type: NodePort